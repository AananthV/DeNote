// Uses racer-schema (https://github.com/derbyparty/racer-schema)

import schemas from './Schemas'
import formats from './Formats'
import validators from './Validators'

export default {
	// Schemas use JSON-Schema format
	schemas,
	formats,
	validators
};
